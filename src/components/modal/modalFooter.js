import React from "react";
import { ModalFooter } from "../../styles/modal/main";

const ModalPartFooter = (props) => {
	return <ModalFooter>{props.children}</ModalFooter>;
};

export default ModalPartFooter;
